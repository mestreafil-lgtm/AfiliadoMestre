{
  "name": "shoppemestre-vitrine",
  "version": "1.0.0",
  "private": true,
  "description": "Vitrine Afiliado Mestre — API Shopee + Supabase",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "build:css": "tailwindcss -i ./uploads/tailwind.input.css -o ./uploads/tailwind.css --minify",
    "build:js": "terser uploads/storefront.js -c -m -o uploads/storefront.min.js && terser uploads/admin.js -c -m -o uploads/admin.min.js",
    "build": "npm run build:css && npm run build:js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "jimp": "^1.6.1"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17",
    "terser": "^5.49.2"
  }
}
